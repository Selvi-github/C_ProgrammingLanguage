We can't apply increment or decrement operators on constants if we do will get an compilation error 
eg:
// Online C compiler to run C program online
#include <stdio.h>

int main() { 
   int  a=100++;     //100=100+1
    printf("%d",a);
    
    return 0;
}
ERROR!
/tmp/JseXdsh14r/main.c: In function 'main':
/tmp/JseXdsh14r/main.c:5:10: error: lvalue required as increment operand
    5 |     a=100++;
      |          ^~


=== Code Exited With Errors ===
eg2:
// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int  a=1;
   while(a++<=1)
   while(a++<=2)
    printf("%d ",a);
    printf("%d ",a);
    return 0;
}
output:
3 5 

=== Code Execution Successful ===
eg3:
// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int  a=1;
   while(a++<=1);
   while(a++<=2);
    printf("%d ",a);
    printf("%d ",a);
    return 0;
}
output:
4 4 

=== Code Execution Successful ===

eg:4
// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int  a=1;
   while(a++<=1);
   while(a++<=2)
    printf("%d ",a);
    printf("%d ",a);
    return 0;
}
output:
4 

=== Code Execution Successful ===

// Online C compiler to run C program online
#include <stdio.h>

int main() {
   int  a=1;
   do
   printf("A");
   while(a++<=1);
   while(a++<=2)
    printf("%d ",a);
    printf("%d ",a);
    return 0;
}
output:
AA4 

=== Code Execution Successful ===
